# =====================================================================
#  GITIGNORE - Selenium Java Cucumber Framework
# =====================================================================

# ---------------------------------------------------------------------
# BUILD (Maven)
# ---------------------------------------------------------------------
target/
build/
out/
*.class
*.jar
*.war
*.ear

# Reportes generados por tests
target/surefire-reports/
target/cucumber-reports/
target/allure-results/
target/allure-report/
target/test-output/
target/screenshots/
target/logs/

# Usuarios generados en runtime (dinamicos)
target/generated-users.json

# ---------------------------------------------------------------------
# GRID STANDALONE (no versionar binarios)
# ---------------------------------------------------------------------
selenium-server-*.jar
selenium-grid/

# ---------------------------------------------------------------------
# CONFIGURACION - Secretos (NUNCA versionar)
# ---------------------------------------------------------------------
.env
.env.local
.env.*.local
*secrets*.properties
*credentials*.properties
local.properties

# NOTA: Los .properties de configuracion publica
# (qa.properties, staging.properties, allure.properties)
# SI se versionan porque NO contienen secretos.

# ---------------------------------------------------------------------
# IDE - Visual Studio Code
# ---------------------------------------------------------------------
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# ---------------------------------------------------------------------
# IDE - IntelliJ IDEA
# ---------------------------------------------------------------------
.idea/
*.iml
*.iws
*.ipr
out/

# ---------------------------------------------------------------------
# IDE - Eclipse
# ---------------------------------------------------------------------
.settings/
.classpath
.project
.metadata/

# ---------------------------------------------------------------------
# MAVEN
# ---------------------------------------------------------------------
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar

# ---------------------------------------------------------------------
# LOGS
# ---------------------------------------------------------------------
*.log
logs/

# ---------------------------------------------------------------------
# SISTEMA OPERATIVO
# ---------------------------------------------------------------------
.DS_Store
Thumbs.db
Desktop.ini

# ---------------------------------------------------------------------
# EDITORES
# ---------------------------------------------------------------------
*.swp
*.swo
*~
*.bak
*.tmp
*.orig

# ---------------------------------------------------------------------
# DOCKER
# ---------------------------------------------------------------------
docker-compose.override.yml

# ---------------------------------------------------------------------
# PYTHON (para futuros subproyectos)
# ---------------------------------------------------------------------
__pycache__/
*.py[cod]
venv/
.venv/
.pytest_cache/

# ---------------------------------------------------------------------
# NODE (para futuros subproyectos)
# ---------------------------------------------------------------------
node_modules/
package-lock.json
yarn.lock